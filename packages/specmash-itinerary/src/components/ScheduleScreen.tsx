import React, { useState, useEffect } from 'react'
import { observer } from 'mobx-react-lite'
import { Text } from '@rneui/themed'

export const ScheduleScreen = observer(({ navigation }: any) => {
	return <Text>ScheduleScreen</Text>
})
